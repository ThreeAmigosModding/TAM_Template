--[[ server config file ]]--

config_server = {}

-- Bot token if needed. Just an example for more sensitive information that shouldn't be passed to the client side.
config.Token = 'ND'

-- rest of your sensitive config options.